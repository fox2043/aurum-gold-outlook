# AURUM SIGNAL 静态托管说明

本目录为纯静态站点，可上传到任意静态托管服务（GitHub Pages、对象存储、Nginx、Vercel Static 等）。

## 入口文件

- `index.html`：站点入口
- `app.js`：页面逻辑
- `styles.css`：视觉样式
- `data/dashboard.json`：行情、模型预测和资讯快照

## 更新数据

必须同时上传 `data-quality.js`。页面每15秒尝试读取腾讯公开报价，每5分钟重新读取 `data/dashboard.json`；刷新页面不等于来源有新报价。

GitHub Pages 本身不采集资讯。仓库中的 Actions 定时任务负责生成 JSON：行情计划每5分钟，资讯与具名机构观点计划每6小时。GitHub 调度可能延迟，不提供精确周期或逐笔行情保证。两个任务共享写入锁，防止互相覆盖。

部署到其他静态主机时，必须另行运行仓库的采集脚本并将更新后的 JSON 同步到该主机；只上传一次 ZIP，新闻和预测不会永久自动更新。需要服务器或 CI 定时运行，顺序为行情、历史K线、国内行情、资讯、专家、预测。所有脚本位于 scripts/，Node 20 可运行。

国内栏使用招行官网转发的上金所 Au99.99，页面标注源时间，非银行账户金或支付宝产品成交价。XAU 使用腾讯/新浪两个公开接口，源时间相差不超过2分钟才计算偏差，超过0.3%暂停预测。两者可能共享上游，并非独立交易所保证。COMEX 是不同品种，不参与现货核验。

资讯与机构观点优先保留国内原文链接，新增新华财经大宗栏目；金十和 Google News 作为补充。采集检查时间与文章发布日期分开保存，缺少源日期的内容不进入近期信息流。专家摘要为媒体转述，不能视为专家对本站模型的认可。

专家权重保留50%；仅近7日具名机构的明确短期观点进入计算。资料不足显示提示。模型不输出未经验证的置信百分比；新增前瞻留档，满5个后续交易日后比较预测误差与价格不变基线。当前版本尚不能声称命中率提高。

专家观点为公开网页的可访问快照，展示前应核对原文中的机构、作者、发布日期和完整上下文。请勿将媒体转述视为投资建议或实际交易信号。

本项目为非商业研究展示；不包含交易、账户或报价执行功能。
